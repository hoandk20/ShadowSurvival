{
  "name": "rune-pixel-survivors",
  "version": "1.0.0",
  "private": true,
  "description": "Rune Pixel Survivors mobile wrapper and build scripts",
  "scripts": {
    "build:mobile-web": "node scripts/build-mobile-web.mjs",
    "cap:sync": "npm run build:mobile-web && npx cap sync",
    "cap:sync:android": "npm run build:mobile-web && npx cap sync android",
    "cap:open:android": "npx cap open android",
    "apk:debug": "npm run build:mobile-web && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.1",
    "@capacitor/cli": "^7.1.1",
    "@capacitor/core": "^7.1.1",
    "phaser": "^3.90.0"
  }
}
